# 第6章 データモデルとデータ統合

## 学習目標

- 概念・論理データモデルの役割を説明できる。
- エンティティ、属性、関係、主キー、外部キーを読み取れる。
- 複数データの粒度、範囲、識別子、形式を比較できる。
- 根本的な識別問題と表面的な形式問題を区別できる。

**シラバス対応**：科目A・その他分野「データベース」、科目B 1-2。

## 1. モデルは業務とデータをつなぐ

データモデルは、業務上の対象、その性質、対象間の関係を整理したものです。

| モデル | 主な目的 | 表す内容 | 読み手の問い |
|---|---|---|---|
| 概念データモデル | 業務の全体像を共有する | 顧客、契約、商品などの主要概念と関係 | 何を管理し、概念同士はどう関係するか |
| 論理データモデル | 要件をデータ構造へ詳細化する | 属性、識別子、正規化された関係 | 何で識別し、どの属性をどこへ持つか |
| 物理データモデル | 特定環境へ実装する | テーブル、列、型、索引、配置 | どの製品・構成で性能と運用を実現するか |

シラバス案は概念・論理モデル、E-R図、UMLを用語例としています。本試験の対象者には、複雑なDB設計より、モデルの意図を理解して業務上の不整合を説明する力が重要です。

## 2. E-R図の基本

- **エンティティ**：管理対象。例：顧客、注文、商品。
- **属性**：対象の性質。例：顧客名、注文日。
- **主キー**：エンティティの各実体を一意に識別する項目。
- **外部キー**：別エンティティの主キーを参照し、関係を表す項目。
- **カーディナリティ**：1対1、1対多、多対多などの関係数。

```text
顧客 1 ─── 多 注文 1 ─── 多 注文明細 多 ─── 1 商品
```

注文と商品を直接結び付けるだけでは、数量や単価をどこに持つか説明できません。注文明細という関係上の単位が必要です。

UMLのクラス図でも、クラス、属性、関連、多重度を用いて似た構造を表せます。表記法が違っても、「一つの顧客は注文を0件以上持てる」「各注文は必ず一つの顧客に属する」のように、最小数と最大数を業務ルールとして読めることが重要です。

| 多重度の例 | 意味 | 確認すべき業務ルール |
|---|---|---|
| `1` | 必ず一つ | 注文は顧客なしで登録できないか |
| `0..1` | なくてもよく、最大一つ | 解約日は未解約なら空でよいか |
| `0..*` | 0件以上 | 顧客は注文履歴がなくても登録できるか |
| `1..*` | 1件以上 | 注文は最低一つの明細を必要とするか |

## 3. 統合前の比較

複数データを結合する前に次を比較します。

| 観点 | 確認例 |
|---|---|
| 意味 | 同じ「売上」が受注額か入金額か |
| 粒度 | 顧客単位か契約単位か |
| 範囲 | 国内のみか、解約顧客を含むか |
| 時点 | 現在値か月末スナップショットか |
| 識別子 | 全社一意かシステム内一意か |
| 形式 | 電話番号のハイフン、日付、文字コード |
| 制約 | NULL、一意性、許容コード |
| 品質 | 欠損、重複、誤入力の程度 |
| 利用条件 | 個人情報、第三者提供、目的制限 |

## 4. 形式一致と意味一致

電話番号のハイフンを除去すれば形式は揃います。しかし、それが本人確認済みで一意か、家族共用や再割当てがあるかによって、顧客識別子としての妥当性は変わります。

逆に、二つの「顧客ID」がどちらも主キーでも、別々のシステムが独自発番していれば、値が同じことに意味はありません。主キーは表の中で一意であることを保証しますが、組織横断で同じ対象を示す保証ではありません。

## 5. 名寄せとマスターデータ

名寄せは、氏名、住所、電話番号など複数属性を正規化・比較し、同一対象らしさを判定します。

注意点：

- 同姓同名を誤結合する偽陽性
- 表記ゆれにより結合できない偽陰性
- 属性の変更、欠損、共有
- 閾値と人手確認
- 個人情報利用の権限と目的
- 判定ルールと結果の説明可能性

恒久的には、全社顧客ID、対応表、マスターデータ管理などを検討します。暫定的に別属性で結合する場合は、精度、対象外、誤判定リスクを明示します。

## 6. データフローとアーキテクチャ

どのシステムが正本か、どこで加工され、誰へ提供されるかを可視化します。データフローは、品質問題の伝播、権限制御、更新遅延、責任境界を理解する助けになります。サブジェクトエリアモデルは、顧客、商品、会計など大きな業務領域でデータを整理します。

```text
[営業システム] --顧客・受注--> [統合処理] --> [DWH] --> [営業BI]
      正本候補                     │             └--> [需要予測]
                                  └--エラー記録--> [運用監視]
```

矢印には、データ名、頻度、変換、責任者、失敗時の扱いを付けます。「システムAからDWHへ」のような箱と矢印だけでは、どの粒度のデータがいつ届き、失敗時に前回値が残るのかを判断できません。

**実例：月次売上の誤集計**

店舗データは店舗・日単位、ECデータは注文単位であり、さらに店舗の「売上」は税込、ECは税抜だったとします。列名をそろえて単純に結合すると、粒度と定義の差により比較を誤ります。共通の分析粒度と金額定義を決め、変換ルール、対応キー、対象期間、除外条件を記録してから統合します。

## 7. サブジェクトエリアで責任境界を設計する

サブジェクトエリアは、単なるシステム一覧ではなく、顧客、商品、契約、販売、会計など、比較的安定した業務概念のまとまりです。各領域について正本、責任者、共通識別子、他領域との受渡しを定めると、システム更改があっても全体構造を説明しやすくなります。

```text
                共通顧客ID                 共通商品ID
[顧客エリア] 1 ───────── 多 [販売エリア] 多 ───────── 1 [商品エリア]
     │                         │                          │
     │ 顧客区分                │ 注文・明細               │ 商品分類
     v                         v                          v
[マーケティング]          [会計エリア] <──仕訳規則── [価格・税ルール]

境界を越える受渡しごとに、意味・粒度・キー・頻度・責任者を定義する。
```

| 設計項目 | 顧客エリアの例 | 販売エリアの例 | 境界で確認すること |
|---|---|---|---|
| 中心となる対象 | 顧客、世帯、連絡先 | 注文、注文明細、返品 | 顧客と注文の対応数 |
| 正本 | 顧客マスター | 受注管理 | 更新競合時の優先元 |
| 基本粒度 | 顧客1件 | 注文明細1件 | 集計前後の粒度 |
| 共通キー | 全社顧客ID | 注文ID＋明細番号 | ローカルIDとの対応表 |
| 責任者 | 顧客管理部門 | 営業業務部門 | 不一致の判断者 |

領域を細かく分けすぎると受渡しが増え、逆に「全社データ」の一領域へまとめると責任が曖昧になります。業務上の意思決定、ライフサイクル、責任者がまとまる単位を基準に境界を置きます。

## 8. マッピング仕様は統合契約である

マッピング仕様は「元列Aを先列Bへコピーする」一覧ではありません。意味、粒度、識別子、変換、品質、例外処理を、提供側と受入側が検証できる形で記述した統合契約です。

| 項目 | 記述例 | 検証観点 |
|---|---|---|
| 元・先 | `ORDER_LINE.qty` → `sales_line.quantity` | 型、NULL可否、単位が一致するか |
| 業務定義 | 確定済み明細の販売数量 | 取消・返品を含むか |
| 粒度 | 注文ID＋明細番号につき1行 | キー重複がないか |
| 変換式 | `qty * unit_price`、端数は明細ごとに切捨て | 境界値と丸め順序は正しいか |
| 参照対応 | 店舗商品コードを共通商品IDへ変換 | 未対応コードをどう扱うか |
| 時点・頻度 | 毎日02:00、前日確定分 | 遅着・再送時に重複しないか |
| 例外処理 | 不正値は隔離し処理件数へ計上 | 黙ってNULLや0へ変えないか |
| 照合条件 | 入力件数＝出力＋隔離、金額差0円 | 完了を客観的に判定できるか |

変換仕様には正常例だけでなく、NULL、未知コード、重複、遅着、取消し、再実行を含めます。仕様にない値を便宜的に0へ変えると処理は成功して見えますが、欠損と実際の0を区別できなくなります。

## 9. 統合パターンを目的で使い分ける

| パターン | 特徴 | 適する場面 | 主な注意点 |
|---|---|---|---|
| バッチETL/ELT | 一定期間分をまとめて抽出・変換 | 日次集計、履歴蓄積 | 遅延、再実行、二重取込 |
| CDC | 追加・変更・削除を継続取得 | 正本の変更追随 | 更新順序、削除表現、取りこぼし |
| メッセージ／イベント | 発生事実を非同期配信 | 注文発生など即時連携 | 重複配信、順序、冪等性 |
| API連携 | 要求時に最新情報を取得・更新 | 顧客照会、在庫引当 | タイムアウト、契約版、呼出し制限 |
| データ仮想化 | 元データを移さず論理的に参照 | 横断検索、短期的な統合 | 元性能、可用性、意味の統一 |

どの方式でも識別子や定義の不一致は自動では解決しません。リアルタイム化は鮮度を上げますが、誤った対応付けを速く広げる可能性もあります。配信方式と意味統合を別々に設計します。

## 10. 粒度とカーディナリティによる誤集計を防ぐ

結合前に各表の「1行が何を表すか」を一文で書き、候補キーの一意性と結合後の件数を検証します。特に、1対多の表を二つ同時に親表へ結合すると、多対多の組合せが生じて金額が水増しされます。

**実例：キャンペーン別売上の水増し**

ある注文に明細が2件、適用キャンペーンが3件あるとします。注文をキーに明細表とキャンペーン表を直接結合すると、`2 × 3 = 6` 行になります。明細金額100円と200円が各3回現れ、単純合計は本来の300円ではなく900円です。

```text
注文 O1
 ├─ 明細: L1=100円, L2=200円       （注文1 : 明細2）
 └─ 施策: C1, C2, C3              （注文1 : 施策3）

危険な結合: 注文 ─ 明細 ─ 施策
             2行 × 3行 = 6行 → 売上が3倍

安全な例  : 明細を注文単位へ集計 ─┐
             施策数を注文単位へ集計 ─┴→ 注文単位同士を結合
```

| 検査 | 実施方法 | 発見できる問題 |
|---|---|---|
| 粒度宣言 | 「1行＝注文の1明細」のように明記 | 異なる単位の混同 |
| キー一意性 | 候補キー別の件数と重複数を確認 | 想定外の多側 |
| 結合前後件数 | 親件数、結合件数、未対応件数を比較 | 行の増殖・欠落 |
| 金額照合 | 元合計と統合後合計を範囲別に比較 | 重複加算、除外漏れ |
| カーディナリティ制約 | 期待する1対1、1対多をテスト | マスター重複、対応表の期間重複 |

複数キャンペーンへ売上を配賦したい場合は、先に配賦ルールを決め、キャンペーン別比率の合計が1になることを検証します。重複を見つけてから `DISTINCT` で消す方法は、正当な同額明細まで消したり、どの関係が誤っていたかを隠したりするため、根本対策ではありません。

## 11. 実務ガイド：連携方式の選択と照合・再実行

連携方式は「リアルタイムが望ましい」だけで決めず、許容遅延、変更の種類、問い合わせ応答、再処理範囲、元システムへの負荷から選びます。CDC（Change Data Capture、変更データ取得）はデータベースの追加・更新・削除を追跡する方式、API（Application Programming Interface、アプリケーション・プログラミング・インターフェース）は要求と応答の契約に従って機能やデータを呼び出す接点です。

| 方式 | 選ぶ条件 | 遅着・障害への備え | 不向きな条件 |
|---|---|---|---|
| バッチ | 分・日単位の遅延を許容し、期間単位で再計算できる | 処理窓、再実行単位、締切を記録 | 取引中に即時応答が必要 |
| ストリーム | 発生順に近い形で継続処理したい | イベント時刻、水位標、重複排除 | 厳密な全体確定を毎件要求 |
| CDC | 正本データベースの変更と削除を追随したい | ログ位置、更新順、再取得手順を保持 | 業務イベントの理由も必要 |
| API | 利用者の要求時に照会・更新したい | タイムアウト、再試行、呼出し制限を設計 | 大量履歴を毎回取得する用途 |

一つに限定する必要はありません。在庫引当はAPI、注文発生はストリーム、分析基盤への変更反映はCDC、日次の全件照合はバッチという組合せもあります。ただし、各経路の正本と優先順位を決めないと、同じ注文が異なる順序で到着したときに古い値で上書きされます。

### 遅着データを確定処理から分離する

イベントの発生時刻と到着時刻を別々に持ちます。水位標（watermark）は「この時刻以前の通常到着はおおむね処理済み」と判断する境界です。締切後の到着を捨てず、暫定値への追補、次期間への繰越し、過去期間の再計算のどれを行うか、業務ルールで決めます。

### 照合を完了条件にする

送信成功やジョブ終了だけでは統合完了とはいえません。処理窓ごとに件数、金額、識別子集合、最大更新時刻を送信側と受信側で比較し、差分を「処理中」「隔離」「遅着」「欠落」に分類します。総件数が一致しても、取消しと新規が一件ずつ入れ替われば内容は異なるため、重要データではキー別の照合も行います。

### 冪等な再試行にする

冪等性とは、同じ要求を複数回実行しても最終状態が一回実行時と同じになる性質です。イベントIDや「注文ID＋更新版」などの冪等性キーを保存し、既処理なら二重加算せず同じ結果を返します。受信記録と業務更新を同じトランザクションで確定するか、途中失敗から安全に再開できる状態遷移を設けます。

```text
[正本] --イベントID・発生時刻--> [受信記録] --> [検証] --> [業務表]
   │                                  │            │          │
   └--処理窓の期待値------------------┴--隔離------+          │
                                      │                       │
                               [遅着キュー] --追補/再計算------+

再試行: 同じイベントID → 受信済みなら再加算しない
照合  : 正本のキー集合 = 業務表のキー集合 + 隔離中 + 未到着
```

**実例：注文変更を日次売上へ反映する**

注文サービスは発生時にストリームへイベントを出し、分析基盤はCDCで注文表の更新も取得します。日次売上は翌日02:00に暫定確定し、バッチで正本と照合します。処理の主経路を注文イベント、欠落回復をCDC、最終確認をバッチと定め、同じ更新が両経路から届いても「注文ID＋更新版」で一度だけ反映します。

ある日の正本には更新が1,250件あり、締切までに1,242件が到着しました。照合結果は未到着8件で、処理失敗0件、隔離0件です。8件を欠落と決めつけず遅着キューで待ち、到着後に同じ日付へ追補すると、対象件数は1,242＋8＝1,250件になります。

その後、タイムアウトした送信側が100件を再送しても、保存済みの冪等性キーを照合するため件数と金額は増えません。最終確定では、正本と受信側の1,250件についてキー集合、取消し状態、売上合計を比較します。不一致が残れば自動で成功扱いにせず、差分IDを隔離して担当者と再処理期限を記録します。

## 12. スキーマ変更を互換性と移行期間で管理する

連携中の列やメッセージ形式を変更するとき、提供側の処理が成功しても利用側が壊れることがあります。変更を「追加だから安全」と決めつけず、現在利用中の契約版、必須条件、未知項目の扱い、移行期限を確認します。

| 変更例 | 主な互換性リスク | 安全な進め方 |
|---|---|---|
| 任意項目を追加 | 厳格な利用側が未知項目を拒否 | 契約テストで旧利用側を確認 |
| 必須項目を追加 | 旧提供側が値を送れない | 任意で追加し、充足後に必須化 |
| 項目名・型を変更 | 旧利用側が読めない、桁落ちする | 新項目を併設して移行 |
| コード値を追加 | 想定外値を0やその他へ誤変換 | 未知値の隔離・通知を定義 |
| 項目を削除 | 利用先の計算・画面が停止 | 利用状況を確認し廃止予告 |

```text
版1: [旧項目] ------------------------> [旧利用側]
                 |
                 +-- 併設期間 --+
                                  v
版2: [旧項目 + 新項目] -----------> [新利用側へ移行]
                                  |
                         契約テスト・利用確認
                                  v
版3: [新項目] <--------- 旧利用ゼロ・承認後に旧項目を廃止
```

提供側と利用側が同じリポジトリにいなくても、代表的な要求・応答やメッセージを使う**契約テスト**で、型、必須項目、コード値、桁、NULLの扱いを自動確認できます。ただし、テストに登録されていない利用者は見えないため、アクセス記録、カタログ、担当者確認も組み合わせます。

**職場例：金額項目を整数円から小数対応へ変える**

海外取引に対応するため、整数の `amount_yen` を小数と通貨を持つ `amount`、`currency` へ変更するとします。直ちに旧列を削除すると、国内請求システムが停止します。まず新列を追加し、国内円取引では新旧金額が一致すること、外貨では換算前金額と通貨が入ることを照合します。

移行台帳で利用先、担当者、契約版、テスト結果、切替日を追い、旧列を読む利用がゼロになった後にオーナーが廃止を承認します。移行期間中の二重書込みに差が出た場合は新値を黙って優先せず、対象を隔離して変換規則を修正します。これにより、停止を避けながら変更を完了し、旧形式を無期限に残すことも防げます。

## 13. 履歴データを有効期間と記録時点で統合する

「現在の値」だけを上書きすると、過去時点の報告を再現できません。一方、変更のたびに行を追加するだけでは、どの期間にどの値が有効だったか分からず、期間重複による多重結合も起きます。履歴統合では、業務上その値が有効な期間と、システムがその事実を知って記録した時点を区別します。

| 時間の観点 | 問い | 項目例 | 主な用途 |
|---|---|---|---|
| 有効時間 | 現実の業務でいつからいつまで正しかったか | `valid_from`, `valid_to` | 過去時点の価格・所属を再現 |
| 記録時間 | システムへいつ登録・訂正されたか | `recorded_at`, `superseded_at` | 遅着や訂正前の報告を説明 |
| 発生時刻 | 取引やイベントがいつ起きたか | `event_at` | 日次・月次の対象期間を決定 |
| 処理時刻 | 連携処理がいつ受信・変換したか | `processed_at` | 遅延監視、再処理調査 |

```text
業務上の有効期間（訂正後に確定した事実）
価格100円: |---- 4/01 ---------------- 4/15 ----|
価格115円:                              |---- 4/16 -------->

システムへの記録
4/01: 100円を登録
4/20: 「4/16から120円」を遅れて受信（誤った版）
4/22: 「4/16から115円が正しい」と訂正

            有効時間を直す          記録時間は消さない
[120円の版] -----------------> [115円の訂正版]
       \-------- どの報告が旧版を使ったか追跡 --------/
```

設計と判断の手順は次のとおりです。

1. 何の履歴を再現するかを決める。価格、所属、住所など、目的によって必要な履歴は異なる。
2. 有効開始・終了の境界を定義する。終了日を含むか、含まないかを統一する。
3. 同じ対象について有効期間が重複しない制約と、空白期間を許す条件を決める。
4. 遅着・訂正時に、過去集計を再計算するか、次期に調整するか、注記だけにするかを業務責任者が選ぶ。
5. 統合時は取引の発生時刻が含まれる履歴行を1件だけ結合し、0件・複数件なら隔離する。
6. 訂正前の版、記録時刻、承認、影響した出力を残し、再計算後に件数と金額を照合する。

**職場例：遅れて届いた価格改定を売上へ反映する**

商品Pは4月1日から15日まで100円、4月16日から115円が正しいとします。しかし価格連携が遅れ、分析基盤には4月20日に「4月16日から120円」と届き、4月22日に115円へ訂正されました。4月16日から19日までに1日10個、計40個を販売していた場合、120円版の売上は `40 × 120 = 4,800円`、正しい売上は `40 × 115 = 4,600円`、差額は200円です。

担当者は現在価格を115円へ上書きするだけで終えず、120円版の記録期間と訂正理由を保持します。4月16日から19日の売上を再計算し、訂正前後の件数40個と差額200円を照合し、その期間の速報レポート利用者へ訂正を通知します。取引と価格履歴の結合で2行以上に一致した場合は、`DISTINCT` で隠さず有効期間の重複として隔離します。これにより、現在値の正しさだけでなく、「4月20日時点のレポートがなぜ4,800円だったか」も説明できます。

## 確認問題（本書オリジナル）

### 問1

ECと店舗の顧客表には、それぞれ重複しない顧客IDがあります。この二つをIDで結合できない理由を説明してください。

**解答例：** 一意性の範囲が各システム内に限られ、同じ顧客へ共通のIDが割り当てられている保証がないためです。組織横断の識別子または妥当な対応付けルールが必要です。「どちらも主キーだから結合できる」という判断は、主キーの有効範囲を取り違えているため不適切です。

### 問2

「顧客1―多 注文」という関係から読み取るべき業務ルールを挙げ、図だけでは不足する点を説明してください。

**解答例：** 一人の顧客に複数注文が対応し、各注文は一人の顧客へ対応することを表します。ただし、注文0件の顧客を許すか、顧客未確定の注文を許すかは最小多重度の確認が必要です。「多」を見て件数上限まで決まったと考えるのは不適切です。

### 問3

二つの売上表で日付形式と通貨表記を統一しました。これだけで安全に統合できない理由を説明してください。

**解答例：** 売上の定義、税込・税抜、受注・入金の別、粒度、対象範囲、取消しの扱い、識別子、利用条件が一致するとは限りません。形式統一は必要でも意味一致を保証しません。列型が一致したことだけを統合可否の根拠にする方法は不適切です。

### 問4

注文表（1注文1行）へ、注文明細表（1注文に複数行）と決済表（分割払いにより1注文に複数行）を注文IDで結合したところ、売上合計が増えました。原因の確認方法と修正方針を説明してください。

**解答例：** まず各表の1行の意味と候補キーを明記し、注文IDごとの明細件数・決済件数、結合前後の行数と金額を照合します。明細と決済がともに注文の多側なら、直接結合によって明細件数×決済件数の組合せが生じています。分析目的が注文単位なら、それぞれを注文単位へ集計してから1対1で結合します。明細と決済の対応が必要なら、決済配賦を表す橋渡し表と配賦ルールを設計し、比率合計を検証します。最後に期待カーディナリティ、未対応件数、元合計との一致を自動テストします。結合後に `DISTINCT` をかける方法は、正当な複数明細を消す可能性があり、粒度の不一致を解決しないため不適切です。

## 主な参照資料

- [IPA：データマネジメント試験（仮称）シラバス（案）Ver.0.1](https://www.ipa.go.jp/shiken/syllabus/minaoshi/rcu1hd000001g4ri-att/syllabus_dm_ver0_1.pdf)
- [デジタル庁：政府相互運用性フレームワーク（GIF）](https://github.com/JDA-DM/GIF)
- [OMG：Unified Modeling Language 2.5.1](https://www.omg.org/spec/UML/2.5.1/PDF/)

## まとめ

- モデルはデータ構造の背後にある業務上の意味を示す。
- 統合前に意味、粒度、範囲、時点、識別子、品質、利用条件を比較する。
- 形式を揃える処理と、同じ対象を識別する仕組みは別問題である。
- サブジェクトエリアの境界とマッピング仕様で、正本、責任、受渡し条件を明確にする。
- 結合前に粒度とカーディナリティを宣言し、件数・金額の照合で行の増殖を防ぐ。
- スキーマ変更は契約テストと併設期間で互換性を確認し、利用先の移行後に旧形式を廃止する。
- 履歴統合では有効時間と記録時間を分け、期間重複、遅着、訂正時の再計算を管理する。
